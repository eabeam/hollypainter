[build]
base = "site-preview"
command = "echo 'Static site — no build needed'"
publish = "."

# Hugo build config (for when Option B theme is ready):
# base = "site-educenter"
# command = "hugo --gc --minify"
# publish = "public"
# [build.environment]
# HUGO_VERSION = "0.156.0"
# HUGO_ENV = "production"
# HUGO_ENABLEGITINFO = "true"
