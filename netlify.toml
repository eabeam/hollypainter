[build]
base = "site-preview"
command = "echo 'Static site — no build needed'"
publish = "."

# Previous config (archived to branch archive/site-educenter-v1):
# base = "site-educenter"
# command = "hugo --gc --minify"
# publish = "public"
# [build.environment]
# HUGO_VERSION = "0.147.2"
# HUGO_ENV = "production"
# HUGO_ENABLEGITINFO = "true"
